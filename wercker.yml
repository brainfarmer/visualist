box: elixir

# Dev pipeline
dev:
  steps:
    - script:
      name: test
      code: |
        mix local.hex --force
        mix test
        
# Build pipeline
build:
  steps:
    - script:
        name: test
        code: |
          mix local.hex --force
          mix test
          
  after-steps:
    - slack-notifier:
        url: $SLACK_URL
        usernamre: wercker-bot
          
